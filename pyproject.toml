[project]
name = "teamsnap-integration"
version = "0.1.0"
description = "Python integration with TeamSnap API v3"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.31.0",
]

[dependency-groups]
dev = [
    "ipython>=8.12.0",
    "pytest>=7.3.1",
]
